# Arène pro, animée, skinnable — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Écran de bataille sans chevauchement, décor animé (torches, braises, bannières, lumière), cases réactives, et arène extraite en scène skinnable (`scenes/arenas/arena_dungeon.tscn` + `arena.gd`).

**Architecture:** Un thème d'arène = une scène Godot portant `arena.gd` (géométrie de grille exportée + `cell_rect()`). `battle.tscn` instancie l'arène ; `battle_scene.gd` perd ses constantes `GRID_*`/`ARENA_*`. Animations en Godot natif (CPUParticles2D, PointLight2D, shader), jamais de spritesheets IA. Assets statiques manquants générés via Playwright MCP → chatgpt.com (compte Pro de l'utilisateur).

**Tech Stack:** Godot 4.5.1 (exe : `C:/Users/rgagu/Downloads/Godot_v4.5.1-stable_mono_win64/Godot_v4.5.1-stable_mono_win64/Godot_v4.5.1-stable_mono_win64_console.exe`), GDScript, Playwright MCP.

## Global Constraints

- Règle n°1 CLAUDE.md : structure UI dans les `.tscn`, scripts = logique seulement (les widgets répétés/dynamiques restent instanciés par code, c'est l'exception admise).
- Texte du jeu en français ; fichiers écrits en UTF-8 explicite (piège PowerShell 5.1).
- Chaque PNG ajouté : réimport `godot --headless --path . --import`, committer le `.png.import` avec le PNG, provenance dans `tools/asset_manifest.json`.
- Vérif après chaque tâche : 40 tests headless verts (`godot --headless --path . -s res://tests/run_tests.gd`) + capture `godot --path . res://scenes/battle.tscn --resolution 1920x1080 -- --autoplay --screenshot=<png>`.
- Le core (`scripts/core/`) n'est pas touché.

---

### Task 1 : Extraction de l'arène (iso-visuel)

**Files:**
- Create: `scripts/ui/arena.gd`
- Create: `scenes/arenas/arena_dungeon.tscn`
- Modify: `scenes/battle.tscn` (remplacer le node `Background` par l'instance d'arène)
- Modify: `scripts/ui/battle_scene.gd:8-27, 194-213, 260-271` (déléguer la géométrie)

**Interfaces:**
- Produces: `class_name BattleArena` avec `cell_rect(cell: Vector2i) -> Rect2` (coordonnées locales à l'arène, qui est full-rect → identiques aux coordonnées écran), exports `grid_seps_y: Array[float]`, `grid_xl_top/xl_bot/xr_top/xr_bot: float`, `board_pos: Vector2`, `board_scale: float`. Le node instancié s'appelle `%Arena` dans `battle.tscn`.

- [ ] **Step 1 : écrire `scripts/ui/arena.gd`**

```gdscript
class_name BattleArena
extends Control
## Base commune des scènes d'arène (scenes/arenas/*.tscn). Porte le décor
## peint et la géométrie de la grille ; battle_scene.gd lui demande où sont
## les cellules au lieu de coder des constantes. Un nouveau thème = une
## nouvelle scène avec ce script, textures échangées, exports ajustés.

## Géométrie de la grille, en pixels texture du plateau peint (BoardPlate).
## Le plateau est un léger trapèze : bords gauche/droit interpolés haut→bas.
@export var grid_seps_y: Array[float] = [145.0, 340.0, 540.0, 745.0, 955.0]
@export var grid_xl_top := 270.0
@export var grid_xl_bot := 190.0
@export var grid_xr_top := 1165.0
@export var grid_xr_bot := 1245.0
## Position/échelle du plateau peint dans l'arène (mêmes valeurs que le
## node BoardPlate ; exportées pour que cell_rect() reste pur calcul).
@export var board_pos := Vector2(312, 60)
@export var board_scale := 950.0 / 1050.0


## Rectangle écran d'une cellule sur la grille peinte.
func cell_rect(cell: Vector2i) -> Rect2:
	# Rangées ennemies en haut (rangée 3 d'abord), joueur en bas.
	var screen_row := 3 - cell.y
	var y0: float = grid_seps_y[screen_row]
	var y1: float = grid_seps_y[screen_row + 1]
	var t := ((y0 + y1) / 2.0 - grid_seps_y[0]) / (grid_seps_y[4] - grid_seps_y[0])
	var xl := lerpf(grid_xl_top, grid_xl_bot, t)
	var xr := lerpf(grid_xr_top, grid_xr_bot, t)
	var pitch := (xr - xl) / 3.0
	return Rect2(board_pos + Vector2(xl + cell.x * pitch, y0) * board_scale,
			Vector2(pitch, y1 - y0) * board_scale)
```

- [ ] **Step 2 : écrire `scenes/arenas/arena_dungeon.tscn`**

Pour cette tâche iso-visuelle, l'arène affiche le fond composé actuel tel quel (une seule TextureRect). Les couches séparées arrivent en Task 3.

```
[gd_scene load_steps=3 format=3 uid="uid://arena_dungeon0"]

[ext_resource type="Script" path="res://scripts/ui/arena.gd" id="1"]
[ext_resource type="Texture2D" path="res://assets/sprites/ui/pixel/battle_bg.png" id="2"]

[node name="Arena" type="Control"]
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
mouse_filter = 2
script = ExtResource("1")

[node name="Environment" type="TextureRect" parent="."]
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
mouse_filter = 2
texture = ExtResource("2")
expand_mode = 1
stretch_mode = 6
```

- [ ] **Step 3 : brancher dans `scenes/battle.tscn`**

Dans l'en-tête, remplacer l'ext_resource texture id="2" par la scène d'arène, et remplacer le bloc node `Background` (lignes ~71-81) par une instance :

```
[ext_resource type="PackedScene" path="res://scenes/arenas/arena_dungeon.tscn" id="2"]
...
[node name="Arena" parent="." instance=ExtResource("2")]
unique_name_in_owner = true
```

(garder `BackgroundFallback` tel quel juste après). Supprimer aussi les 4 nodes `RowMarker*` de battle.tscn ? **Non** — les garder mais ils seront repositionnés par script en Task 3 ; ne pas y toucher ici.

- [ ] **Step 4 : déléguer dans `scripts/ui/battle_scene.gd`**

Supprimer les constantes `ARENA_POS`, `ARENA_SCALE`, `GRID_SEPS_Y`, `GRID_XL_TOP`, `GRID_XL_BOT`, `GRID_XR_TOP`, `GRID_XR_BOT` (lignes 14-24) et le corps de `_cell_rect` (260-271). Remplacer :

```gdscript
@onready var arena: BattleArena = %Arena
```

(à la place de `@onready var background: TextureRect = %Background` ; adapter `_init_ui()` : le test `if background.texture == null` devient inutile → `bg_fallback.visible = false` reste piloté par la présence de l'arène, garder simplement le fallback invisible par défaut et supprimer le test)

```gdscript
## Rectangle écran d'une cellule (délégué à la scène d'arène du thème courant).
func _cell_rect(cell: Vector2i) -> Rect2:
	return arena.cell_rect(cell)
```

- [ ] **Step 5 : vérifier iso-visuel**

```powershell
$godot = "C:/Users/rgagu/Downloads/Godot_v4.5.1-stable_mono_win64/Godot_v4.5.1-stable_mono_win64/Godot_v4.5.1-stable_mono_win64_console.exe"
& $godot --headless --path . -s res://tests/run_tests.gd          # attendu : 40/40 OK
& $godot --path . res://scenes/battle.tscn --resolution 1920x1080 -- --autoplay --screenshot="<scratchpad>/t1_after.png"
```

Comparer visuellement avec la capture d'avant (`battle_autoplay.png`) : identique.

- [ ] **Step 6 : commit**

```powershell
git add scripts/ui/arena.gd scenes/arenas/arena_dungeon.tscn scenes/battle.tscn scripts/ui/battle_scene.gd
git commit -m "refactor(arena): arene extraite en scene skinnable arena_dungeon.tscn"
```

---

### Task 2 : Génération des assets manquants (Playwright → ChatGPT Pro)

**Files:**
- Create: `assets/backgrounds/battle_env_dungeon.png` (+ `.import`)
- Create: `assets/sprites/ui/arena/panel_frame_ornate.png`, `btn_gold.png`, `btn_steel.png`, `hand_tray.png` (+ `.import`)
- Modify: `tools/asset_manifest.json` (provenance)

**Interfaces:**
- Produces: PNGs aux chemins ci-dessus, consommés par Task 3 (layout) et Task 4 (panneaux).

- [ ] **Step 1 : générer le décor d'environnement**

Via les outils MCP `mcp__playwright__browser_*` : naviguer sur chatgpt.com (session Pro déjà connectée), uploader `assets/mockup/game exemple.png` comme référence de style, prompt (adapter si le résultat déçoit) :

> Décor de fond 1920×1080 pour un jeu de cartes tactique dark-fantasy en pixel-art 16-bit peint (même style que l'image jointe) : intérieur de donjon en pierre vu de dessus en légère plongée, sol dallé sombre, mousse, végétation sur les bords, éclairage chaud de torches sur les côtés, VIGNETTAGE sombre sur les bords. IMPORTANT : PAS de plateau de jeu, PAS de grille, PAS de personnages, PAS de texte — uniquement l'environnement vide, le centre reste sombre et discret (un plateau sera posé par-dessus).

Télécharger, recadrer/redimensionner en 1920×1080 exact (Pillow dans `tools/` si besoin), enregistrer en `assets/backgrounds/battle_env_dungeon.png`.

- [ ] **Step 2 : générer le kit UI pro**

Même session, même référence de style, générations séparées (une image par asset, fond transparent demandé explicitement) :

1. `panel_frame_ornate.png` — « Cadre de panneau UI rectangulaire vertical ~600×900, pierre sombre + liserés dorés + coins ornés, CENTRE VIDE/TRANSPARENT semi-opaque sombre, style pixel-art 16-bit peint, PNG transparent, pas de texte » (sera utilisé en NinePatch : bords 48 px).
2. `btn_gold.png` / `btn_steel.png` — « Bouton UI rectangulaire ~360×96, plaque métal [doré chaud / acier sombre] avec rivets et relief, centre lisse pour du texte, pixel-art 16-bit, PNG transparent, pas de texte ».
3. `hand_tray.png` — « Tablette de bois sombre horizontale ~1400×220 avec liseré doré discret, vue de face, pixel-art 16-bit, PNG transparent, pas de texte » (support visuel de la main de cartes).

- [ ] **Step 3 : importer et tracer**

```powershell
& $godot --headless --path . --import
```

Ajouter les entrées dans `tools/asset_manifest.json` (source : "chatgpt-pro via playwright", date, prompt court).

- [ ] **Step 4 : commit**

```powershell
git add assets/backgrounds/battle_env_dungeon.png* assets/sprites/ui/arena/ tools/asset_manifest.json
git commit -m "feat(assets): decor environnement donjon + kit UI pro (chatgpt pro)"
```

---

### Task 3 : Layout sans chevauchement (couches séparées)

**Files:**
- Modify: `scenes/arenas/arena_dungeon.tscn` (Environment = nouveau décor, + node `BoardPlate` = `assets/sprites/ui/mockup/board_arena.png`)
- Modify: `scenes/battle.tscn` (panneaux repositionnés, `HandArea` en bande réservée)
- Modify: `scripts/ui/battle_scene.gd` (row markers positionnés depuis la géométrie)

**Interfaces:**
- Consumes: `BattleArena.cell_rect()` (Task 1), `battle_env_dungeon.png` (Task 2).
- Produces: nouvelle géométrie exportée dans `arena_dungeon.tscn` : `board_pos = Vector2(160, -46)`, `board_scale = 0.92` (valeurs initiales, à affiner visuellement).

- [ ] **Step 1 : couches dans `arena_dungeon.tscn`**

- `Environment.texture` → `battle_env_dungeon.png`.
- Nouveau node sous Environment :

```
[node name="BoardPlate" type="TextureRect" parent="."]
layout_mode = 0
offset_left = 160.0
offset_top = -46.0
offset_right = 1477.0    # 160 + 1432*0.92
offset_bottom = 920.0    # -46 + 1050*0.92
mouse_filter = 2
texture = ExtResource("<board_arena.png>")
expand_mode = 1
stretch_mode = 5
```

- Exports du node racine : `board_pos = Vector2(160, -46)`, `board_scale = 0.92` (mêmes valeurs que BoardPlate — contrat documenté dans arena.gd).

- [ ] **Step 2 : cibles de layout dans `battle.tscn`** (1920×1080)

| Zone | Avant | Après |
|---|---|---|
| EnemyPanel (ADVERSAIRE) | 20→280 | 20→280 (inchangé) |
| Grille jouable | ~484→1439 (sous le panneau joueur !) | ~335→1305 |
| PlayerPanel (JOUEUR) | 1330→1630 (chevauche INFOS) | offset_left 1330, offset_right **1596** |
| InfoPanel (INFOS) | 1586→1900 | offset_left **1612**, offset_right 1900 |
| HandArea | chevauche la rangée 0 | offset : left 300, top **855**, right 1620, bottom 1080 |
| LogPanel | (vérifier) | sous InfoPanel, même colonne 1612→1900 |

`custom_minimum_size` du PlayerPanel : `Vector2(266, 0)` (au lieu de 300) pour respecter offset_right.

- [ ] **Step 3 : row markers depuis la géométrie**

Dans `_init_ui()` (battle_scene.gd), après la création des cellules :

```gdscript
	# Numéros de rangée alignés sur la grille du thème courant.
	for r in GameConst.BOARD_ROWS:
		var marker: TextureRect = get_node("RowMarker%d" % r)
		var rect := _cell_rect(Vector2i(0, r))
		marker.position = Vector2(rect.position.x - 44, rect.get_center().y - 17)
```

- [ ] **Step 4 : vérifier**

Capture `--autoplay` : plus aucun élément du plateau sous les panneaux, main sous la rangée 0, panneaux JOUEUR/INFOS disjoints. Itérer sur `board_pos`/`board_scale`/offsets si besoin (grille dans x∈[300,1310], y∈[70,850]). Tests 40/40.

- [ ] **Step 5 : commit**

```powershell
git add scenes/ scripts/ui/battle_scene.gd
git commit -m "feat(ui): layout bataille sans chevauchement, decor en couches"
```

---

### Task 4 : Habillage pro des panneaux et de la main

**Files:**
- Modify: `resources/ui_theme.tres` (StyleBoxTexture panel_frame_ornate en variation `PanelBattle`, boutons btn_gold/btn_steel)
- Modify: `scenes/battle.tscn` (theme_type_variation sur les 4 panneaux, `HandTray` TextureRect dans HandArea)

**Interfaces:**
- Consumes: PNGs de Task 2.
- Produces: variation de thème `PanelBattle` réutilisable par toute scène.

- [ ] **Step 1 :** dans `ui_theme.tres`, ajouter une `StyleBoxTexture` (texture `panel_frame_ornate.png`, marges texture 48 px, content margins 20) sous la variation de type `PanelBattle` (PanelContainer) ; boutons : `StyleBoxTexture` btn_gold (normal/hover/pressed par modulate) sur `Button`, btn_steel sur `ButtonSecondary`.
- [ ] **Step 2 :** appliquer `theme_type_variation = &"PanelBattle"` aux nodes EnemyPanel, PlayerPanel, InfoPanel, LogPanel, LegendPanel dans battle.tscn. Ajouter dans HandArea (premier enfant) un TextureRect `HandTray` full-rect (`mouse_filter = 2`, texture hand_tray.png, stretch keep-aspect-centered).
- [ ] **Step 3 :** capture + comparaison au mockup `game exemple.png` ; tests 40/40 ; ajuster marges si le texte touche les bords.
- [ ] **Step 4 : commit** — `feat(ui): panneaux ornes et tablette de main (kit chatgpt)`

---

### Task 5 : Ambiance animée (dans `arena_dungeon.tscn` uniquement)

**Files:**
- Create: `scripts/ui/torch_light.gd` (flicker), `resources/shaders/banner_wind.gdshader`
- Modify: `scenes/arenas/arena_dungeon.tscn`

**Interfaces:**
- Produces: rien pour les autres tâches — tout est interne au thème (c'est le but : un autre thème peut tout remplacer).

- [ ] **Step 1 : shader vent** `resources/shaders/banner_wind.gdshader`

```glsl
shader_type canvas_item;
// Ondulation de bannière : fixe en haut (UV.y=0), amplitude croissante vers le bas.
uniform float amplitude = 6.0;
uniform float speed = 2.2;

void vertex() {
	VERTEX.x += sin(TIME * speed + UV.y * 4.0) * amplitude * UV.y;
}
```

- [ ] **Step 2 : flicker de torche** `scripts/ui/torch_light.gd`

```gdscript
extends PointLight2D
## Vacillement de torche : bruit lissé sur l'énergie de la lumière.

@export var base_energy := 1.1
@export var flicker := 0.35

var _t := randf() * 10.0


func _process(delta: float) -> void:
	_t += delta
	energy = base_energy + flicker * (
			sin(_t * 7.3) * 0.5 + sin(_t * 13.1 + 1.7) * 0.3 + sin(_t * 3.7) * 0.2)
```

- [ ] **Step 3 : nodes dans `arena_dungeon.tscn`** (tout posé dans l'éditeur/tscn, Règle n°1)

- `Props/BannerL`, `Props/BannerR` : TextureRect `banner_red.png` (mockup/) posés exactement sur les bannières peintes du BoardPlate, material ShaderMaterial banner_wind.
- `Props/TorchL`, `Props/TorchR` : Node2D positionnés sur les flammes peintes (~texture (72, 520) et (1364, 520) → écran via board_pos/scale, à caler à l'œil) contenant chacun :
  - `Flame` CPUParticles2D : amount 26, lifetime 0.7, direction (0,-1), gravity (0,-140), initial_velocity 28-46, scale 2.5→0.5, color_ramp orange (#ffdf8a → #ff8c2e → #802010 alpha 0), emission sphère r=7.
  - `Embers` CPUParticles2D : amount 8, lifetime 2.2, gravity (0,-60), spread 25°, scale 1.2, couleur #ffb347, alpha fade.
  - `Light` PointLight2D script torch_light.gd, texture = GradientTexture2D radiale 256 px blanche→transparente, `texture_scale 2.2`, `color #ffb066`, blend ADD.
- `Ambience/Dust` : déplacer le CPUParticles2D `Dust` existant de battle.tscn vers l'arène (poussière lumineuse qui monte, déjà réglée).
- `Ambience/Fireflies` : CPUParticles2D plein écran, amount 14, lifetime 8, vitesse 6-14, scale 1.6, couleur #ffe9a8 alpha 0.5, pour la profondeur.

- [ ] **Step 4 :** capture : flammes visibles aux deux torches, lumière chaude qui vacille (2 captures espacées → énergies différentes), bannières ondulent (2 captures → décalage). Tests 40/40. Vérifier le coût : `--autoplay` toujours fluide.
- [ ] **Step 5 : commit** — `feat(arena): ambiance animee du theme donjon (torches, braises, bannieres, lumiere)`

---

### Task 6 : Cases réactives

**Files:**
- Modify: `scripts/ui/board_cell.gd`, `scenes/widgets/board_cell.tscn`

**Interfaces:**
- Consumes: rien de nouveau. L'API `set_highlight(mode)` existante est conservée telle quelle.

- [ ] **Step 1 :** dans `board_cell.tscn`, `mouse_default_cursor_shape = 2` (pointing hand) sur le node racine.
- [ ] **Step 2 :** dans `board_cell.gd` :

```gdscript
var _pulse: Tween


func _ready() -> void:
	_apply_style()
	mouse_entered.connect(_on_hover.bind(true))
	mouse_exited.connect(_on_hover.bind(false))


func _on_hover(on: bool) -> void:
	# Survol : la cellule s'éclaire doucement (feedback même sans highlight).
	var target := Color(1.18, 1.18, 1.12) if on else Color.WHITE
	create_tween().tween_property(self, "modulate", target, 0.12)


func set_highlight(mode: String) -> void:
	highlight = mode
	_apply_style()
	if _pulse != null:
		_pulse.kill()
		_pulse = null
		_ring.modulate.a = 1.0
	# Cases jouables : pulsation douce qui attire l'œil sans crier.
	if mode in ["summon", "move", "attack", "target"]:
		_pulse = create_tween().set_loops()
		_pulse.tween_property(_ring, "modulate:a", 0.45, 0.65)\
				.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
		_pulse.tween_property(_ring, "modulate:a", 1.0, 0.65)\
				.set_trans(Tween.TRANS_SINE).set_ease(Tween.EASE_IN_OUT)
	elif mode == "selected":
		_pulse = create_tween().set_loops()
		_pulse.tween_property(_ring, "modulate:a", 0.7, 0.4)
		_pulse.tween_property(_ring, "modulate:a", 1.0, 0.4)
```

(remplace le `set_highlight` et le `_ready` actuels ; garder `_apply_style` inchangé)

- [ ] **Step 3 :** vérif : lancer une partie fenêtrée (`godot --path . res://scenes/battle.tscn`), survoler/sélectionner ; capture autoplay ; tests 40/40.
- [ ] **Step 4 : commit** — `feat(ui): cellules reactives (survol, pulsation des cases jouables)`

---

### Task 7 : Vérification finale

- [ ] Tests 40/40, smoke `--autoplay` complet sans warning watchdog.
- [ ] Capture 1920×1080 comparée au mockup `game exemple.png` : zéro chevauchement, ambiance vivante.
- [ ] Capture mulligan (lancement sans `--autoplay`) : l'overlay reste correct.
- [ ] `/graphify --update` (changement structurel : nouveaux fichiers arena.gd, arena_dungeon.tscn).
- [ ] Mettre à jour la mémoire `battle-arena-geometry.md` (géométrie désormais exportée dans arena_dungeon.tscn, plus de GRID_* dans battle_scene.gd).
